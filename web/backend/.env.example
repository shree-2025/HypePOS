# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=hypepos

# Auth
JWT_SECRET=supersecret_jwt_key_change_me
BCRYPT_ROUNDS=10

# SMTP (for sending outlet credentials)
SMTP_HOST=localhost
SMTP_PORT=1025
# SMTP_USER=
# SMTP_PASS=
SMTP_FROM="HypePOS <no-reply@hypepos.local>"
